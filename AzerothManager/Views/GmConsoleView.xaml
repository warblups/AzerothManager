<UserControl x:Class="AzerothManager.Views.GmConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource Bg}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Raccourcis -->
        <ItemsControl Grid.Row="0" ItemsSource="{Binding Shortcuts}" Margin="0,0,0,8">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding}" Margin="0,0,6,6"
                            Command="{Binding DataContext.UseShortcutCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Sortie -->
        <Border Grid.Row="1" BorderBrush="{StaticResource BorderB}" BorderThickness="1" Background="{StaticResource InputBg}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Output}" Margin="8">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="12"
                                       TextWrapping="NoWrap" Margin="0,0,0,1" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Saisie -->
        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="CommandBox" Grid.Column="0" FontFamily="Consolas" Margin="0,0,6,0"
                     Text="{Binding Command, UpdateSourceTrigger=PropertyChanged}"
                     KeyDown="CommandBox_KeyDown" />
            <Button Grid.Column="1" Content="Envoyer" Style="{StaticResource AccentButton}"
                    Command="{Binding SendCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource NotBool}}" />
            <Button Grid.Column="2" Content="Effacer" Command="{Binding ClearCommand}" Margin="0" />
        </Grid>
    </Grid>
</UserControl>
